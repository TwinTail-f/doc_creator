# Publisher Test Resources

Static fixture files for publisher unit tests.
Mirrors the layout of `tests/unit/parser/resources/`.

Subdirectories:
- `html/`  — HTML fixture files (to be extracted from inline Python constants)
- `pages/` — Confluence page snapshots for integration-style tests
