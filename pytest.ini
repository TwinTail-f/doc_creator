[pytest]
addopts = -vv -s --cov=autodoc --cov-report html
markers =
    business_logic: business-domain rule verification
    integration: two or more real collaborators working together
    contract: protocol/interface compatibility
    infrastructure: framework mechanics, subprocess, HTTP internals
